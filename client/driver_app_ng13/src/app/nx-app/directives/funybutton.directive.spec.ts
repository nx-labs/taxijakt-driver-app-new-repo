import { FunybuttonDirective } from './funybutton.directive';

describe('FunybuttonDirective', () => {
  it('should create an instance', () => {
    const directive = new FunybuttonDirective();
    expect(directive).toBeTruthy();
  });
});
