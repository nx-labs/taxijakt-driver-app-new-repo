import { BoxshadowDirective } from './boxshadow.directive';

describe('BoxshadowDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxshadowDirective();
    expect(directive).toBeTruthy();
  });
});
